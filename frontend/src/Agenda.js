import React, { Component } from 'react';

class Agenda extends Component {
  constructor(props) {
    super (props);
  }

  render() {
    return (
      <div>

      </div>
    );
  }
}

export default Agenda;
